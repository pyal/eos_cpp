#ifndef _ANSYSTEM_TYPES_H
#define _ANSYSTEM_TYPES_H

void AddSimpleType(const char *name, Program_exe &prog) {
    type_def *def = new type_def(name);
    prog.types.prg_types[name] = def;
    oper_def *oper_new = new oper_def(NEW);
        oper_new


	prog.types.operations.push_front(
}
Storage() {
    AddSymbol();
    GetSymbol();
    PushBlock();
    PopBlock();
    PushTmpSymbol();
    PopTmpSymbol();
    //	list.add(symbol);
}
Analyzer() {
    GetTypeDef(list<names>) Go() {
        SumLexems();
    }
}
#endif
